/**
 * The above type defines an object with a single property "rules" which is an array of strings
 * representing CSS rules.
 * @property {string[]} rules - `rules` is a property of the `StyleSheetData` type, which is an array
 * of strings representing CSS rules. These rules can be applied to HTML elements to style them in a
 * certain way. The `StyleSheetData` type is commonly used in web development frameworks and libraries
 * to define styles for components
 */
export type StyleSheetData = {
  rules: string[];
};
